#

## Passagem por referência:

- Objetos e Arrays
- Recebe uma cópia referência idêntica ao valor de memória original
- Afeta original

## Passagem por valor:

- Número, Valor, String
- Cópia independente
- Não afeta o original (endereço de memória diferente)

## Objeto

- Conjunto de chave e valor
- delete: remove propriedades

Fila

- Contador
- Ponteiro para primeiro elemento
- Elemento (string/num)

Início:
Contador = 0
Primeiro Elem = 0
items {} objeto vazio

para adicionar:
verificamos se existe algum elemento,
se não existir, o primeiro será undefined
e também incrementamos o contator
count ++
adicionamos um nome de item indexado com count,
para formar par de chave-valor de um objeto
0: estojo
1: caneta

remover elemento:
sempre temos que remover
