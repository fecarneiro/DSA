/*
 * O(n²)
 * Conceito de organizar cartas na mão
 * Dividir mentalmente array em: parte organizada(esquerda) e desorganizada(direita)
 * Um elemento por vez -> inserir na posição correta
 * Consideramos arr[0] como já "organizado"
 */

function insertionSort(arr) {
  for(i = 1; i < arr.length ; i++)

}

const arr = [3, 2, 5, 4, 1];
